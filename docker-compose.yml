version: '3'

services:
  clamav_rest:
    build: .
    networks:
      - overlay
    ports:
      - "8090:8090"
  clamav:
    image: mk0x/docker-clamav:alpine
    networks:
      - overlay
    ports:
      - "3310:3310"

networks:
  overlay:
